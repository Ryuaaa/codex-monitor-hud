# Codex Monitor HUD 1.1.0

这是macOS正式版1.1.0。重点是把官方额度、本地Token时间窗和可选提醒整合到原有悬浮窗，不删除任务活动、最近任务、费用、预测和电脑性能监控。Windows继续使用独立更新通道，不受本次macOS版本影响。

本次更新：

- Codex额度优先读取官方 `account/rateLimits/read`，区分实时、本轮未返回但仍有效、已过期和官方触顶。
- 新增5小时、滚动24小时和当前官方每周周期的本地Token对比。本地数据未覆盖完整周期时，不显示容易误导的周占比。
- 新增可选的完整额度列表、官方恢复次数、官方触顶类型和历史单日峰值Token。
- 新增周额度消耗提醒：默认关闭，支持自然日或滚动24小时、1%–100%阈值、悬浮窗内提示和可选静音系统通知。数据不足或仅有旧数据时不触发，每个官方重置周期最多通知一次。
- OpenAI服务状态改为默认关闭，开启时才请求官方状态页。
- 成本缓存升级为v5，保留1.0统计的安装后费用历史，新的5分钟Token时间桶从升级后开始积累。

1.0已有的主要能力：

- 新增本机Token历史、30天费用估算、本月费用预测，以及5小时/每周额度趋势预测。
- 账户Token统计与安装后费用统计相互独立；累计高水位规则避免重复或交错快照放大费用。
- Token历史低优先级增量扫描；首次分批补齐，完成后只读取新增内容，关闭模块后停止扫描。
- 官方账户Token统计保留为可选项，本机Token与费用成为默认显示。
- 主页高度按所选模块自动增长，不再裁掉底部内容。
- 鼠标拖动任意边角连续等比缩放；取消150%固定上限，最大尺寸按屏幕可用空间决定。
- 增加单实例保护；源码安装时先正常退出旧实例，避免登录启动服务和手动实例重复运行。
- 新增最近任务历史与“任务活动（本机推测）”，明确区分官方历史数据和本机会话文件趋势判断。
- 优先保存官方状态数据库中的最新任务工作目录；会话历史迁移、压缩或暂不可读时不再误报为0个活跃任务。
- Codex账户和任务列表空闲时5分钟刷新，低额度且有任务活动或读取失败时1分钟刷新；任务活动在5秒与20秒间自动切换。
- Token接口尚未返回今天数据时，显示最近有数据的日期和用量，不再显示“今日待结算”或错误的0。
- 设置页集中显示全部勾选状态，支持主页模块排序，并补充数据来源与权限说明。
- 新增低饱和灰蓝线稿IP应用图标及可重复生成的全尺寸ICNS、单实例测试、完整主页布局测试、长文字测试和更新重试测试。
- 新安装默认不随登录自动启动；有需要时可在设置中开启，升级会保留旧用户已有的选择。
- 新增OpenAI官方服务状态，辅助判断异常来自Codex官方服务、本机网络还是账号；该模块低频刷新且可关闭。
- 将本机Token缓存压缩为按日期和模型的最小统计，保留逐次计价结果和不可逆路径摘要，显著降低长期刷新开销并避免长上下文误计价。
- 构建产物明确支持macOS 15.0及以上，并使用Developer ID强化运行时签名、Apple公证和票据装订。

安装说明：

1. 下载并解压 `Codex-Monitor-HUD.app.zip`。
2. 将应用移到个人“应用程序”文件夹后打开。
3. 应用已经Developer ID签名并通过Apple公证，可由macOS直接验证开发者身份和安装包完整性。

系统要求：macOS 15或更高版本。Codex账户数据需要本机已安装并登录ChatGPT桌面版、Codex桌面版或Codex命令行工具；电脑性能监控可独立使用。

Windows 1.1未签名预览版：

- 提供原生Win32三页面悬浮窗、主页模块组合、自由缩放、置顶、任务栏最小化及集中设置。
- 提供整机CPU、内存、网络、磁盘、进程排行，以及Codex/ChatGPT进程树的CPU、内存和进程读写趋势。
- 提供Codex额度、Token日汇总、最近任务、本机任务活动推测、费用趋势、额度预测、服务状态和周额度消耗提醒。
- GitHub Windows虚拟机已经验证编译、完整测试套件、窗口真实启动、MSI安装/卸载、旧版到1.1.0升级、更新安全边界和篡改拒绝。
- 正式发布文件将使用SignPath.io和SignPath Foundation提供的公开可信Windows代码签名；正式工作流会将真实发布者证书固定进一键更新器。签名完成前不创建Windows 1.1正式发布。

Windows系统要求：Windows 10/11 x64。真实用户电脑上的视觉、资源占用、Codex账号数据和任务管理器逐项对照仍待补充。
