# Codex Monitor HUD 1.3.0 · macOS 与 Windows

两端同步计数修复、多语言、多币种和手动订阅日期。任务中心是独立应用，本次不改动。

- 修复子任务继承累计Token导致的重复计数；零基线、异常负数和累计倒退均有回归样例。旧缓存保留恢复副本后重建，原安装统计起点不变。
- 更新OpenAI Standard API参考价格，增加Astra。费用是API等价估算，不是Pro订阅账单；不还原历史优惠、Fast、Batch或工具加价。
- 默认人民币，可切换美元、欧元、日元、韩元；公开参考汇率最多每天更新一次，失败保留上次值并显示日期。
- 内置简体中文、繁体中文、英文、日文、韩文；语言重启生效，币种立即生效。
- 手动填写截止或续费日期，并提供官方账单页入口；不读取浏览器或账单。
- 保留任务活动与历史、性能监控、模块选择、置顶、最小化和缩放。后台额度轻量读取、旧接口回退、过期数据提示与有界统计同步维护。

## 下载及差异

- macOS 15+，Apple芯片与Intel通用：`Codex-Monitor-HUD.app.zip`，Developer ID签名、Apple公证和票据验证。已有用户可在应用内检查更新。
- Windows 10/11 x64：`CodexMonitorHUD-windows-x64-1.3.0.msi`及便携ZIP。本次为未签名预览，可能显示“未知发布者”，不进入正式自动安装通道；请手动下载，不要关闭系统安全功能。
- 每个下载包提供SHA-256校验文件。升级保留设置；缓存重建期间统计可能逐批补齐，缺失记录不猜数。

Windows真实桌面长期验证和可信签名不声称已完成；操作系统特有指标和原生界面细节并非完全相同。
