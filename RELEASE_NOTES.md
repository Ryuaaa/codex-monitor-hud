# Codex Monitor HUD 1.2.0 · macOS

本版完成HUD的正式版本整理、官方额度读取减负和接口兼容升级。任务中心仍为独立应用；Windows本轮不更新。

- 顶栏和菜单提供“打开任务中心”入口，关闭任务中心后不影响HUD常驻监控。
- 后台额度读取省略恢复次数逐项明细；启动和手动刷新完整读取，旧接口自动单次回退。不增加刷新频率。
- 只采用官方返回的普通包含用量许可；缺失或null不猜测，不把余额百分比当成许可。
- 区分接口不支持、认证、限速、超时、网络与协议错误；单项失败保留有效旧值，连接失败渐进退避。
- 线程卸载不是任务完成；不自动恢复、订阅线程或增加常驻服务。
- 原有额度、费用、任务历史、电脑性能、缩放、自定义设置和一键更新保留。

## 安装与升级

需要macOS 15或更高版本，支持Apple芯片与Intel。安装包使用Developer ID强化运行时签名、Apple公证和票据装订。

已有HUD用户可通过“检查更新”下载并安装。新用户下载 `Codex-Monitor-HUD.app.zip`，解压后移到“应用程序”文件夹再打开。`Codex-Monitor-HUD.app.zip.sha256`用于核对文件完整性。

升级保留已有设置和本机统计历史。Codex额度需要本机已安装并登录Codex或ChatGPT；电脑性能监控可独立使用。

任务活动仍为本机趋势判断，不承诺能旁路读取Codex桌面版全部精确运行状态。费用仍是本机估算，不是实际账单。
