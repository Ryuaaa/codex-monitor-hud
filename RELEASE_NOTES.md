# Codex Monitor HUD 1.4.0 · macOS 与 Windows

本版保留现有性能、额度、任务活动、Token 与费用功能。macOS 新增可选的订阅日期同步和菜单栏显示；Windows 保留手动日期核对。任务中心是独立应用，本次不改动。

- macOS 可在独立、短时的官方 ChatGPT 网页窗口登录，尝试读取明确显示的本期到期/续费日期与续订状态。登录空间与 Codex、Chrome 隔离；HUD 只保存日期、状态、核对时间和失败类别。同步失败保留上次有效日期。
- 每日自动尝试同步默认关闭，用户首次核对成功后才可开启；请确认独立窗口登录的是要查看的账号。账单网页或付款渠道不提供明确日期时，请在原付款渠道核对后手动填写。该功能并非官方订阅日期接口，不保证每个账号都可自动读取。
- macOS 可分别显示屏幕悬浮窗和顶部菜单栏，也可同时显示；关闭完整窗口不会强迫菜单栏退出。
- 原有手动填写日期、打开官方账单页、清除日期和账户额度等设置继续可用。

## 下载及差异

- macOS 15+，Apple芯片与Intel通用：`Codex-Monitor-HUD.app.zip`，Developer ID签名、Apple公证和票据验证。已有用户可在应用内检查更新。
- Windows 10/11 x64：本版仍为未签名预览，可能显示“未知发布者”，不进入正式自动安装通道；请手动下载，不要关闭系统安全功能。Windows 保留手动日期设置与官方账单页入口，暂不提供独立网页自动同步。
- 每个下载包提供 SHA-256 校验文件。升级保留既有设置。

Windows 可信签名及本机真实桌面长期验证尚未完成，不把 CI 构建或预览包称作 Windows 正式签名版。
