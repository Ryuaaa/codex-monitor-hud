# macOS HUD 1.2.1 维护修复验收

日期：2026-09-04。范围为上一轮对抗式审查的3项P1、3项P2和正常状态文案。
源码构建号21；已完成Developer ID签名、Apple公证及本机1.2.0→1.2.1替换。尚未提交或发布到GitHub。
不修改任务中心或Windows，不读取真实对话正文、凭据或Cookie，不增加常驻进程或定时器。

## 修复和验证

| 项目 | 修复 | 验证方法 |
|---|---|---|
| P1 异常Token | 检查嵌套元素与非负整数；拒绝null、布尔、错误类型、非有限数、负数、分数和越界值；坏记录不影响下一条记录 | 合法/异常日桶、本地Token、类型边界和计数溢出样例 |
| P1 老任务漏算 | 同一5分钟增量扫描中补查旧日期目录的修改时间；额外遍历20000项上限；路径标准化兼容/private/var、/var及重复斜杠，缓存仍用原逻辑路径摘要 | 40天前目录中的近期1000 Token可见，重复扫描仍为1000；普通和系统临时目录均验证 |
| P1 更新回滚 | 当前可信完整应用副本作短时启动探测，复制后先验签；同一PID连续五次完成启动才清理旧版；失败恢复前暂停匹配的已启用登录服务，避免反复拉起故障版；原本关闭的服务保持关闭 | 生产脚本隔离测试，完整签名应用副本/无效副本测试，以及本机正式签名包实际升级；健康成功、启动失败、未就绪、签名失败、错误团队、早崩、反复重启、旧版不退出、无法停止故障进程、登录服务回退与保留关闭状态 |
| P2 部分用量归零 | 只有有效返回的字段替换旧值；没有日桶不刷新日桶时间；显式空数组与数字0仍有效；界面说明保留旧值 | 仅summary更新时，原7天/30天值和更新时间不变；可选峰值缺失保留，显式0更新 |
| P2 缓存异常重置 | 写前大小上限；原子写入与上一版有效备份；异常恢复时保持原起点；无有效备份时明确不可用并保留文件与内存快照 | 超过8MiB、截断JSON、备份恢复、主备同时损坏；原起点保持不变 |
| P2 缺失加快轮询 | 按显示模块选择接口；正常缺失不触发临时故障退避；隐藏模块无需等待对应回复 | 仅额度、仅任务请求；空可选字段保持300秒；结构化网络错误仍可退避 |
| 正常文案 | 改为“未发现明显资源压力”“仅依据已监测指标” | 现有主页、长文字、缩放和布局诊断 |

## 本机检查

- 原生arm64/x86_64双架构构建，编译警告视为错误；最终包使用Developer ID签名。
- `sh tests/test-codex-protocol.sh`：原有73个校验点通过。
- `sh tests/test-maintenance.sh`：新增176个校验点通过，覆盖异常、恢复、完整签名应用副本和无效副本场景，已接入构建与发布前CI，但本轮尚未运行远程CI。
- 逻辑、界面、显隐、缩放、单实例、更新信息与历史报告诊断均通过。
- 使用新启动探测器只读核验已安装HUD；退出边界只向不存在的临时应用路径发起，确认真实HUD的PID不变。
- 旧`--update-handoff-diagnostic`会干扰真实LaunchAgent，已明确停用并以隔离测试替代；不会伪报完成实机升级。

## 真实安装验收

- 2026-09-04完成后台替换。用户应用程序目录中的唯一HUD运行实例为版本1.2.1（21）；旧进程正常退出，既有登录服务继续运行。
- Apple最终公证：`81bd0f57-1d81-4c0e-b183-d6c4dbac072f`，Accepted。安装后`codesign --verify --deep --strict`、`stapler validate`、Gatekeeper `Notarized Developer ID`均通过。
- 安装后再次通过离线逻辑、隐藏界面、显隐、缩放及单实例诊断。37项用户设置不变，35份既有性能历史保留原字节前缀，费用统计起点、Token窗口覆盖起点和额度历史保持不变。
- 最终可执行文件SHA-256：`6028b9e3ac7f05e8506ce4b9c2fe68180c6247fd0c26b9034dd7fea643fc8828`。
- 最终安装包：`/private/tmp/hud-1.2.1-install.GShNu8/final/Codex-Monitor-HUD-1.2.1.app.zip`，SHA-256：`50d13fddc59fd9d7d5d19a2bf30663cb80563077e23e218a11f62968d6fbedfd`。
- 本机回退目录保留旧1.2.0应用压缩包及替换前设置/数值历史备份，恢复资料不上传。只有规范安装路径运行，不把回退压缩包称为第二个运行实例。
- 初次正式签名预检发现：仅复制可执行文件脱离原应用，会丢失签名依赖的Info.plist而被系统拒绝。该次在退出旧HUD前停止，未改安装。改为生产函数复制并验证完整应用，加入合成签名正反例，再重新签名公证最终包；早期候选包不用于安装。

## 边界

- 不追算安装前费用。完整缓存与备份均不可用时，不能无依据恢复原高水位；保留数据、显示问题，而不是重新初始化。
- 旧目录达到元数据上限时按不完整数据处理；本轮没有宣称完成所有体量的长期资源压测。
- 本轮仅完成本机替换，GitHub发布、远程CI及Windows不在本轮范围。
- 从1.2.1发起未来升级使用新回滚机制。旧1.2.0用户自行点击更新仍使用旧更新器；本机此次受控升级提前使用新辅助程序和已核验的1.2.1完整签名探测副本，不能据此宣称旧客户端更新逻辑也已改变。
- 首次测试暴露了系统临时目录的别名/重复斜杠差异；实际路径回读后修正，并保留失败诊断，未通过降低断言使测试通过。
