# P1 验证报告（2026-08-22）

## 当前结论

P0 契约与 P1 只读 MVP 代码已经形成，并在 macOS 完成生产构建、纯逻辑/组件测试、后台视觉检查、受控 20 次进程回收和强制结束隔离检查。P1 尚不能标记“全部验收完成”：Windows 没有本轮真实构建/运行证据；macOS 可见窗口的人工关闭、WebView 崩溃注入和三轮资源基线仍待验证。

## 要求逐项状态

| 要求 | macOS | Windows | 证据/缺口 |
|---|---|---|---|
| 独立 Tauri + React 应用、独立进程 | 已验证 | 仅静态验证 | macOS `.app` 与 release 二进制成功；Windows 共享源码和 CI 尚未运行 |
| 多项目/工作目录映射 | 已验证 | 仅静态验证 | 显式只读 JSON 配置 + 领域回退；组件测试覆盖项目切换 |
| 全项目总览、看板、列表 | 已验证 | 仅静态验证 | 组件测试与 1440/800 两档后台截图 |
| 搜索、状态筛选、紧凑显示 | 已验证 | 仅静态验证 | 组件测试覆盖搜索/视图切换；实际渲染观察 |
| 详情正文按需读取 | 已验证 | 仅静态验证 | 首页调用测试；Rust 读取层权限二次校验；2 MiB 上限 |
| 正式事件时间线 | 已验证 | 仅静态验证 | JSONL 逐行读取，坏行隔离测试 |
| 逾期/即将到期/需要关注 | 已验证 | 仅静态验证 | 时间边界与可解释关键词单测 |
| Codex 对话绑定与安全入口 | 已验证降级 | 仅静态验证 | 展示绑定；无官方稳定第三方打开方式时按钮禁用 |
| 四种 Codex 运行语义 | 已验证 | 仅静态验证 | 四固定样例单测；P1 未接真实桌面实例 |
| 中文优先、未来可国际化 | 仅静态验证 | 仅静态验证 | 中文语义清楚；尚未接完整翻译资源系统 |
| 键盘、焦点、缩放、窗口适配 | 已验证基础项 | 仅静态验证 | 原生语义控件、焦点样式、Esc 关闭、800×600；200% 系统缩放未实机验证 |
| 从未打开无新增服务 | 已验证 | 未验证 | 任务中心未运行时无对应 PID；未安装服务/计划任务/托盘 |
| 关闭后全部退出 | 部分验证 | 未验证 | 20/20 受控进程退出通过；可见窗口关闭按钮待用户预览验证 |
| 无监听端口/Node 运行时 | 已验证 | 未验证 | 首轮真实生产包监听 0、Node 子进程 0；循环前后 Node PID 集合相同 |
| 强制结束不影响 HUD | 已验证 | 未验证 | 任务中心 `SIGKILL` 后 HUD PID 保持 `37549` |
| 坏任务/接口失败不影响 HUD | 仅静态验证 | 未验证 | 单文件隔离与 Provider 错误降级已测；HUD 无依赖关系；未做 WebView 崩溃注入 |
| HUD 资源基线回归 | 未验证 | 未验证 | 需要按相同口径做三轮至少 10 分钟基线；不先定百分比 |

## 已运行检查

- 前端：2 个测试文件、8 个测试全部通过。
- Rust：6 个只读/路径/隐私/坏文件测试全部通过。
- 生产前端：TypeScript + Vite 构建成功。
- macOS：arm64 release 二进制和 `.app` 打包成功；本地为临时 ad-hoc 签名，不是发布包。
- 正式目录最小只读诊断：12 个元数据文件、正文读取 0 字节、写操作 0。
- 生命周期：20/20 受控启动—退出，首轮端口监听 0、Node 子进程 0，结束后任务中心 PID 0。
- 隔离：强制结束任务中心后 HUD 原 PID 不变。
- 视觉：1440×900 四列、800×600 两列，未观察到覆盖或不可达的核心控件。

## 未执行

- 未推送、未发布、未修改 GitHub Release。
- 未写入、移动、重命名或规范化正式任务 Markdown/JSONL。
- 未修改 HUD 源码或入口。
- 未在 Windows runner/真机上构建或运行。
- 未进入 P2 写入能力。

