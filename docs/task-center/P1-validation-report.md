# P1 验证报告（2026-08-24 macOS 最终收口）

## 当前结论

P0 契约与 P1 只读 MVP 已在隔离工作树形成。按用户 2026-08-24 的最终验收，**macOS P1 只读任务中心已正式完成**；Windows 真实桌面验收暂缓，不再阻塞 macOS。macOS arm64 生产包已完成真实窗口、20 次原生打开—关闭、三轮资源基线、Retina 2.0 倍缩放、最小窗口、强制终止和 WebContent 异常隔离验证。

Windows 已在真实 GitHub 托管 runner 完成前端、Rust、Tauri GUI `.exe` 构建，以及精确主窗口 `WM_CLOSE` 的 20 次生命周期、WebView2 启动/回收、监听端口、残留进程与强制终止自动化验证；真实桌面的人工视觉、200% 缩放/最小窗口、资源三轮基线、HUD 共存隔离和 WebView2 崩溃隔离仍暂缓。不能把这些 Windows 缺口外推为 macOS 未完成，也不能把 CI 自动化写成 Windows 桌面体验通过。

## macOS 最终收口（提交 `f6fa772`）

- 分支、上游和工作树起点一致：`codex/task-center-p1` 本地与远端均为 `f6fa772`；收口前工作树干净。原 HUD 工作树仍为 `ebadeb7`，四项用户未跟踪内容保持原状。
- `f6fa772` 只更新 Windows 验证文档，没有修改前端、Rust、Tauri 配置或 macOS 运行代码。本轮重新生成的未重签名 arm64 release 二进制 SHA-256 仍为 `935009601697dcd859681474f6f097bdbd0efe1da7731eb78ca1aab115c19188`，与已完成 macOS 20 次生命周期、窗口和故障隔离验证的产物逐字节相同。
- 本轮重跑前端 10/10、Rust 8/8、生产前端构建、生产依赖审计（0 个已知漏洞）及 macOS `.app` 生成，全部通过。可选 DMG 封装在当前主机报 `hdiutil: create failed - 设备未配置`；最小空目录探针同样失败，界定为当前主机镜像工具限制，不影响 `.app` 本体，也不声称本轮 DMG 成功。
- 重新复核同一二进制的默认窗口与最小窗口截图；结合既有真实交互记录，覆盖看板、列表、项目切换、搜索、状态筛选、详情、正文按需读取、活动时间线、空状态和错误降级。Retina backing scale `2.0`、最大窗口和 760×522 原生最小边界已验证；没有发现文字覆盖、控件重叠或核心布局形变。用户随后查看真实 macOS 界面并明确确认“颜色和视觉没有问题”，主观视觉验收完成。
- 当前桌面控制工具拒绝重新连接任务中心应用，因此本轮没有为了凑次数重复真实点击和故障注入；这项工具限制单独保留。由于当前产物与既有真实验证产物哈希相同，既有 20/20 正常关闭、强制终止、WebContent 定向异常、无监听端口/后台服务/Node 残留以及 HUD 原 PID 不变的证据仍直接适用。
- 正式目录最小只读诊断再次得到：12 个元数据文件、正文读取 0 字节、写操作 0。合成夹具和 Rust 测试继续证明受限 frontmatter 不越过 Rust 边界、正文权限二次校验、坏文件/坏事件行隔离；未写入、移动、重命名或规范化正式资料。
- 三轮资源原始 CSV 仍在，九个 SHA-256 与资源报告完全一致。证据仅支持“没有可辨别的 HUD CPU 或唤醒常驻影响”；任务中心打开时自身 footprint 中位数约 100.83 MiB，关闭后相关进程 footprint 为 0。关闭恢复时 HUD 的 RSS、footprint 和能耗有小幅漂移，落在同方法重复波动范围，不能称为绝对零影响。

## 要求逐项状态

| 要求 | macOS | Windows | 证据/缺口 |
|---|---|---|---|
| 独立 Tauri + React 应用、独立进程 | 已验证 | 已验证 | macOS `.app`/DMG 与 arm64 二进制；Windows GUI `.exe` 与独立 PID/WebView2 进程 |
| 多项目/工作目录映射 | 已验证 | 已验证（自动化） | 两平台组件测试通过；显式只读 JSON 配置 + 领域回退 |
| 全项目总览、看板、列表 | 已验证 | 已验证（自动化） | 两平台组件测试；Windows 尚缺人工桌面视觉验收 |
| 搜索、状态筛选、紧凑显示 | 已验证 | 已验证（自动化） | 两平台组件测试覆盖搜索/视图切换；Windows 尚缺真实键盘体验 |
| 详情正文按需读取 | 已验证 | 已验证（自动化） | 两平台首页调用测试与 Rust 权限二次校验；2 MiB 上限 |
| 正式事件时间线 | 已验证 | 已验证（自动化） | 两平台 Rust JSONL 逐行读取、坏行隔离测试 |
| 逾期/即将到期/需要关注 | 已验证 | 已验证（自动化） | 两平台时间边界与可解释关键词单测 |
| Codex 对话绑定与安全入口 | 已验证降级 | 已验证降级（自动化） | 展示绑定；无稳定官方第三方打开方式时按钮禁用 |
| 四种 Codex 运行语义 | 已验证 | 已验证（自动化） | 两平台四固定样例单测；P1 未伪装成全桌面实例监控 |
| 中文优先、未来可国际化 | 仅静态验证 | 仅静态验证 | 中文语义清楚；尚未接完整翻译资源系统 |
| 键盘、焦点、缩放、窗口适配 | 已验证 | 仅静态验证 | 原生语义控件、焦点样式、Esc；Retina 2.0 倍；1728×998 最大与 760×522 下限实测无重叠，超出看板按设计滚动 |
| 从未打开无新增服务 | 已验证 | 未验证 | 任务中心未运行时无对应 PID；未安装服务/计划任务/托盘 |
| 关闭后全部退出 | 已验证 | 已验证 | macOS 20/20；Windows 精确可见主窗 `WM_CLOSE` 20/20，主进程和本轮 WebView2 均归零 |
| 无监听端口/Node 运行时 | 已验证 | 已验证 | 两平台生产进程监听 0；Windows 前后 Node PID 均为空，任务中心/WebView2 无残留 |
| 强制结束不影响 HUD | 已验证 | 未验证 | macOS HUD 原 PID 不变；Windows 仅证明独立哨兵存活，不冒充 HUD 共存证据 |
| 坏任务/接口失败不影响 HUD | 已验证 | 未验证 | Rust 单文件/坏 JSONL 隔离、React Provider/详情失败测试；HUD 无依赖 |
| WebView 异常不影响 HUD | 已验证 | 未验证 | macOS 已定向注入；Windows 只验证正常/强制退出后的 WebView2 回收，未安全注入 WebView2 崩溃 |
| HUD 资源基线回归 | 已验证 | 未验证 | 三轮 × 三状态 × 每状态 10 分钟；CPU/唤醒分布回到重叠区间，内存/能耗小幅漂移不可归因于任务中心；详见资源报告 |

## 已运行检查

- 前端：2 个测试文件、10 个测试全部通过；覆盖项目切换、看板/列表、搜索、正文按需加载、元数据接口失败与单详情失败隔离。
- Rust：8 个测试全部通过；覆盖元数据只读、受限 frontmatter 不跨 Rust 边界、正文权限、路径逃逸、坏文件、坏事件行、项目映射和正文分隔符。
- 生产前端：TypeScript + Vite 构建成功；生产依赖审计为 0 个已知漏洞。
- macOS：arm64 release 二进制和 `.app` 成功；既有本地 DMG 曾成功，本轮当前主机的 `hdiutil` 封装失败。本地 `.app` 不是发布签名包。
- 真实关闭：20/20 次通过；最后一批 15 次由辅助功能树每轮重新识别原生关闭按钮，不复用过期元素编号。
- 正式目录最小只读诊断：12 个元数据文件、正文读取 0 字节、写操作 0。
- 三轮资源基线：九个窗口均 60 个有效样本、0 缺样；详见 `resource-baseline-2026-08-23.md`。
- 故障隔离：强制结束主进程和定向结束 WebContent 两种故障下 HUD 原 PID 均不变。
- 视觉：Retina backing scale `2.0`；最大窗口与 760×522 原生最小边界实际验证，没有核心控件覆盖。
- Windows CI：GitHub Actions run `32609138427` 两个 job 全绿；Windows 前端 10/10、Rust 8/8、Tauri GUI `.exe`、20/20 精确主窗关闭、WebView2 回收、TCP/UDP 端点、Node/服务/进程残留和强制终止隔离通过。
- Windows 产物：版本 `1.1.0`，PE `Windows GUI`，大小 `9,148,416` 字节，SHA-256 `908c9bbd965471616afe7e619919189a775f7138a5b7fa621cc17b901df85c51`；下载后跨平台校验通过。

## Windows 当前证据

- 真实 runner、job、产物与证据边界见 `windows-ci-evidence-2026-08-23.md`。
- CI 首先发现 `data/` 忽略规则导致任务中心 Provider/夹具未提交；加任务中心白名单后，两平台测试恢复。
- 初版生命周期测试错误依赖 `.NET Process.MainWindowHandle`，控制台子系统下可能命中附带控制台，形成假阳性。最终构建改为 PE GUI 子系统，并枚举 PID 的可见顶层窗，仅对标题 `Codex Monitor 任务中心`、类名 `Tauri Window` 的精确 HWND 发送 `WM_CLOSE`。
- 最终 run `32609138427` Windows 与 macOS job 均成功；Windows 20/20 生命周期报告中的标题、类名、退出码、TCP/UDP 端点、Node/服务/进程和 WebView2 残留均符合门槛。
- Windows WebView2 进程每轮实际出现并回收，但 runner 注册表未返回运行时版本；不声称已验证具体 WebView2 版本。

## 本轮修复

- 权限过滤前移到 Rust：受限或缺少明确访问许可的任务只返回隔离错误，完整 frontmatter 不再传入 WebView。
- 正文分隔符改为只接受独立行 `---`，避免 frontmatter 值或正文内短横线被误判。
- 增加 Provider/单详情失败组件测试和 macOS `proc_pid_rusage` 可复用资源探针。
- Rust 工具链锁定为 `1.88.0`；GitHub `setup-node` 与 `upload-artifact` 固定到使用 Node 24 运行时的官方完整提交 SHA。
- 修复任务中心数据层被仓库通用 `data/` 规则误忽略的问题，并由干净 runner 证明提交内容完整。
- Windows release 标记为 GUI 子系统；生命周期测试按 PID、可见性、标题和窗口类精确选择主窗，消除控制台/内部窗口假阳性。

## 未执行

- 仅按用户新增授权推送 `codex/task-center-p1`；未合并、未创建 Release、未发布或修改更新通道。
- 未写入、移动、重命名或规范化正式任务 Markdown/JSONL。
- 未修改 HUD 源码、入口、发布内容或原工作树中的用户文件。
- 未在人工可观察的真实桌面 Windows 上做视觉、缩放、资源、HUD 共存和 WebView2 崩溃验收。
- 未进入 P2 写入能力。
