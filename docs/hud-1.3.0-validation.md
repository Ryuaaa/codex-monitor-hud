# macOS HUD 1.3.0 本机验收

## 范围

本记录覆盖macOS HUD本机验收：计数修复、CNY默认及USD/EUR/JPY/KRW、简中/繁中/英/日/韩。Windows同步与双平台发布另行验收；任务中心不在本轮范围。

## 计算证据

- 根因：子任务首次记录带有父线程累计量，原算法将其作为新消耗再计一次。修正为建立继承基线，之后只计高水位之上的新增。显式零基线保留下一笔新使用量。
- 首笔究竟包含多少继承量无法区分时，保守跳过该累计样本；旧原始文件缺失的部分无法完整恢复，不声称是账单级全量统计。
- 每条新增记录先计价再压缩，禁止用未知模型的多条聚合量回填成一次长上下文调用。
- 价格基准为2026-09-11 OpenAI Standard参考价，Sol输入/缓存读/缓存写/输出每百万Token为4/0.4/5/20美元，Astra为10/1/12.5/50美元。长上下文按官方表。估值不还原历史促销、Fast、Batch、工具或区域附加费。
- 官方来源：https://developers.openai.com/api/docs/pricing
- 独立副本已完成重新计算，原安装起点保持不变；实际运行值随后继续增加。公开记录不包含用户实际使用量、费用或安装日期。
- 原金额异常不代表实际订阅扣款；不同时间快照不能当成恒定金额逐分比较。

## 已通过

- 原生arm64/x86_64编译，开启Wall/Wextra/Werror。
- 原有19项逻辑诊断、73项协议检查；新增后193项维护检查，包括十亿继承量、重复/倒退累计值、零基线、根线程首笔、Sol/Astra计价、未知价格、缓存迁移备份、重复扫描、安装起点、币种格式。
- 490条文案的en/ja/ko覆盖和格式参数顺序核对；繁中使用macOS内置简繁转换。
- 五种语言分别跑原生界面诊断：显示开关、日期、模块排序、布局、缩放、位置锁定、隐藏模块停止采样、刷新频率与回退状态全部通过。
- 设置截图为合成测试，不是已安装程序运行证据；安装、公证状态须另行回读。

## 资源与隐私

- 无浏览器运行时、无翻译服务、无新增高频定时器。语言包只读加载，Token扫描仍每轮64MB上限。
- 汇率复用现有显示刷新触发，最多每天一次；匿名HTTPS，不发送监控或账号数据。失败不清空旧汇率。
- 旧缓存迁移前备份独立保留；仅处理同一HUD应用标识，不删除任务中心。

## 本机安装

- 已安装用户应用程序目录中的 `Codex Monitor HUD.app`，版本1.3.0，build30。
- Apple公证结果Accepted；提交编号f80d5fb9-b7a8-4e13-bfb8-e3ebed0e34b8。公证票据已装订并验证；安装后再次通过codesign严格检查及spctl，来源Notarized Developer ID。
- 通过既有LaunchAgent恢复运行；最终回读仍为同一进程，已运行约76分钟。单次资源观测不证明长期或跨设备性能。
- 已从实际HUD界面核对修复后的Token、人民币估算和计价覆盖率；不公开用户实际数值。
- 32项已有显示/大小/置顶/订阅日期等偏好与备份逐项一致；原安装统计起点和用户的显示选择未变。
- 本机Applications下仅一个HUD安装；任务中心与试用版均未改动。旧HUD存为可恢复ZIP，不保留另一个可启动的旧HUD应用。
- 本机恢复目录保留旧版ZIP、安装前缓存、安装起点和偏好副本；缓存另有独立迁移前备份。恢复资料不进入GitHub。
- 此记录证明本机版本与签名公证ZIP已验收，不单独证明GitHub发布完成。

## 后续维护边界

价格表会随官方调价过时；当前清楚标注参考日期而不是历史实际账单。未定价记录不猜价，缺失原始记录不伪造。翻译覆盖和原生布局已验证，但没有声称经过五种语言母语用户的长期可用性评测。
