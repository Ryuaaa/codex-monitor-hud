# 更新记录

## 1.0.1 / Windows 0.3.1

- Token 与费用改为以安装时为零点，只统计之后新增的今日、7日、30日和本月数据，不再读取或换算旧历史费用。
- 将 macOS 与 Windows 更新通道分离，避免跨平台版本误判。
- macOS 更新安装前和替换时固定核验项目开发者签名。
- 成本缓存限制为 8MB，并加入零点、迁移、增量和更新通道回归测试。

## 1.0.0

- 新增CodexBar式本机Token历史：今日、7天和30天统计，按模型估算30天API等价费用与本月费用预测；界面只保留一次“估算”标记。
- Token总量优先采用官方每日汇总，本机会话扫描负责模型与计价样本；累计Token改用高水位差值，避免重复快照和交错累计计数被反复放大。
- 正式发布脚本与GitHub工作流增加Developer ID强化运行时签名、Apple公证、票据装订和Gatekeeper验证；未配置正式证书时发布流程会失败关闭。
- 新安装默认不随登录自动启动；已经开启的旧用户升级时保留现有选择。该功能不作为1.0发布门槛。
- 新增OpenAI官方服务状态卡片，分别显示Codex桌面版组件和OpenAI整体状态；正常时10分钟、异常或失败时2分钟检查，隐藏后停止请求。
- 新增5小时与每周额度趋势预测；同一重置窗口至少积累15分钟后，判断按当前速度能否撑到重置。
- 本机Token扫描采用每5分钟低优先级增量缓存、每轮64MB首次补齐上限和512KB单行保留上限；普通正文流式跳过，隐藏模块后停止扫描。
- 本机Token缓存改为按日期和模型压缩，保留逐次计价结果以避免合并后误触发长上下文价格；完整路径改存不可逆摘要，大幅降低长期缓存体积和刷新开销。
- 官方账户Token统计改为可选且默认关闭，本机Token与费用默认显示；两者仍可独立加入主页或Codex页。
- 增加标准价格、长上下文、未知模型、重复记录去重、增量缓存和额度预测测试，并加入真实本机会话扫描诊断。
- 修复主页模块较多时被526点固定高度裁掉的问题；主页现在按实际模块增长，并结合屏幕可用空间限制最大缩放。
- 鼠标拖动边角仍保持等比连续缩放，取消150%固定上限；最大尺寸改为随当前屏幕自动计算，最小75%用于保证标签和按钮可操作。
- 修复长Token说明可能挤压相邻卡片宽度的问题，并增加最长文字与主页全模块布局测试。
- 增加单实例文件锁；安装源码版前会先正常退出旧实例，避免手动运行实例和登录启动服务同时存在。
- 新增最近3个Codex任务的历史列表，显示可选任务标题与更新时间；标题缺失时显示“未命名任务”，不读取任务预览，并明确不代表实时运行状态。
- Codex任务活动改为活跃5秒、空闲20秒智能刷新；账户与任务列表正常或空闲5分钟、低额度且有活动或失败时1分钟刷新，减少空闲时的接口进程启动次数。
- Token接口没有返回今天的数据时，改为显示最近有数据的日期和用量，不再使用容易误解的“今日待结算”。
- 增加正常、过期、失败、显示上次数据等明确状态；单项失败保留最后一次有效值。
- 设置页新增主页Codex模块与电脑模块的拖动排序，以及数据来源和权限说明。
- 最近任务、任务活动、5小时额度与每周额度都可分别加入主页或Codex页；单任务实时Token速度因无法连接桌面版同一App Server实例，继续隐藏而不提供猜测值。
- 应用图标改为用户确认的低饱和灰蓝线稿IP徽章，修正透明圆角，并加入可重复生成全尺寸ICNS的脚本。
- “任务活动（本机推测）”可分别加入主页和Codex页；使用官方任务标题、最近活动时间和可选文件路径，结合本机会话文件最近写入情况，显示活跃数量、最长活动时长和最多3个标题。单个候选最多检查文件末尾1MB，任务列表限制64项；不会把外部 App Server 的 `notLoaded` 状态误写成官方实时运行状态。
- 增加超大任务文件和跨日期长期任务的回归覆盖，避免任务开始记录超出文件末尾或任务目录较旧时被误报为0。
- 保存官方状态数据库返回的最新任务工作目录，不再用可能过期的会话文件目录代表当前项目。
- 兼容Codex会话历史迁移、压缩和暂不可读状态：无法完整读取时显示“无法可靠判断”或“可确认活跃”，不再把缺失记录误报为0个活跃任务；不为此增加新定时器或解压后台任务。

- 正式安装包使用Developer ID强化运行时签名并通过Apple公证、票据装订和Gatekeeper验证。
- 明确固定最低系统版本为macOS 15.0，避免构建机版本意外抬高兼容门槛。
- OpenAI服务状态原生网络读取遇到系统证书链兼容问题时，使用macOS自带安全下载工具进行一次后备读取。

## 0.8.2

- 修复应用内一键更新后，新版本虽已启动，但macOS登录启动服务仍处于“已退出”状态的问题。
- 更新安装程序现在优先通过LaunchAgent重启新版本，并在服务未加载时自动重新加载；没有启用登录启动时才直接打开应用。
- 增加更新后启动服务交接回归测试。

## 0.8.1

- 修复拖拽边角调整整体大小时，窗口仍可能先被拉成错误比例、再由内部布局适应而造成形变的问题。
- 拖拽过程现在由窗口级约束强制保持宽高比，内部界面始终使用固定逻辑尺寸等比缩放。
- 替换原本会掩盖形变的拖拽测试；新测试直接覆盖横向拖拽、纵向拖拽、最大尺寸、真实窗口返回尺寸和内容逻辑边界。

## 0.8.0

- 首个带可下载应用包、SHA-256校验和应用内更新能力的公开预览版。
- 发布包改为同时支持Apple芯片和Intel处理器。
- Codex接口客户端版本自动跟随应用版本，并补充常见Codex命令行工具安装路径。
- Codex命令行诊断会直接给出额度、订阅或用量的失败原因。

- 增加GitHub Release更新检查、每天一次自动提示、手动检查和一键安全更新；下载后校验SHA-256、应用身份、版本与代码签名，失败自动保留旧版。
- 增加位置和大小锁定按钮及设置项，防止常驻悬浮窗被误拖动或误缩放。
- 隐藏且当前不需要的排行榜、网络、交换空间和热状态停止采集；只查看Codex页且关闭历史时暂停电脑采样。
- 为5秒电脑刷新、60秒Codex刷新和每天一次的更新检查加入macOS定时器容差，降低长期运行的唤醒次数。
- 增加标签触发的GitHub Release打包发布流程，以及更新器安全诊断。
- 统一CPU和内存显示口径：新增整机已用内存、总量与百分比；Codex内存同时显示GB和占总内存百分比，Codex CPU明确标注为整机总算力口径。
- 修复拖拽调整悬浮窗大小时卡片、文字和图形可能被横向或纵向拉伸的问题；窗口外框尺寸与内容尺寸现在分开计算，并交由AppKit约束内容宽高比。
- 悬浮窗改为可拖动任意边角连续缩放，范围75%–150%，并提供恢复标准大小。
- 增加可选的最长单次任务时长与历史最长连续天数；主页和Codex页均默认关闭。
- 修复 Codex CPU 时间未按 macOS 机器计时比例换算而被明显低报的问题，并加入固定公式测试和受控单核实测。
- 增加启动、再启动、系统唤醒、正常退出与运行实例标记；异常退出后由 macOS 自动重新启动。
- 趋势报告可区分有生命周期标记解释的缺口与无法解释的缺口。
- 将纯界面组件和布局从主控制文件拆分到独立 `HUDView` 模块。
- 增加关闭模块后的卡片与详细文字回归测试，以及生命周期报告测试。

## 0.7.0

- 增加可自由组合模块的主页，并保留Codex与电脑性能分页面。
- 增加置顶、非置顶和真正最小化到macOS程序栏的按钮；原来的窗口收窄功能保留为窄栏模式。
- 增加标准Mac应用菜单，可显示、最小化、隐藏和退出悬浮窗。
- 电脑性能页增加内存占用最高的5个软件排行。
- 5小时额度与每周额度改为独立显示和独立开关。
- Token统计改为连续自然日口径，并增加日期逻辑自测。
- 修复接口未返回今天数据时被误显示为“今日0”的问题，改为显示待结算与最近统计日期。
- 修复关闭5小时额度或订阅后，状态栏和详细文字仍泄漏对应内容的问题。
- 齿轮改为集中设置窗口，全部页面的勾选状态可以同时查看。
- 增加悬浮窗整体大小85%、100%和120%三档，并保存选择。
- Codex读取改为仅使用公开文档中的稳定账户方法，不启用实验接口。
- 额度、订阅和用量增加独立更新时间与失败状态。
- 增加Codex瓶颈归因置信度和真实CPU趋势采样时长。
- 使用原生AppKit深色半透明界面，不嵌入网页引擎。
