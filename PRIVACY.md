# 隐私说明

Codex Monitor HUD 默认只在本机处理数据。

- 电脑性能数据来自 macOS 系统接口。
- Codex数据来自本机安装的 Codex App Server，请求公开文档中的 `account/rateLimits/read`、`account/read`、`account/usage/read` 和 `thread/list`，分别读取额度窗口、订阅类型、按日Token用量和最近任务元数据。
- 1.2.0后台额度请求仅增加省略恢复明细的参数；普通包含用量许可只在内存保留官方布尔值及时间。不提取、持久化或显示accountId、任意后端横幅、认证元数据或原始错误信息。错误分类只提取白名单错误码与HTTP状态码；接口读取缓冲上限4MB，单轮结束清空。
- 任务活动模块只使用任务编号、可选标题、最近活动时间、当前工作目录和本机会话路径。当前工作目录只在内存中用于项目归属与诊断计数，不在悬浮窗、性能历史或命令行诊断中输出完整路径。官方响应可能同时包含任务预览，但程序不会提取、保存或显示预览内容；标题缺失时只显示“未命名任务”。
- 为判断任务最近是否仍在活动，程序在活跃时每5秒、空闲时每20秒检查最近写入的本机会话文件，单个候选最多读取末尾1MB，只解析记录类型、角色、阶段和时间戳。读取缓冲区可能短暂包含正文或工具输出字节，但程序不会提取、保存、显示或上传这些内容。
- 可选的“Token用量与费用”模块优先使用Codex官方每日Token汇总；每5分钟扫描本机会话文件中的时间、模型和Token计数，仅用于本机模型结构与费用估算。首次补齐每轮最多读取64MB，单行缓冲最多512KB；普通正文不做JSON解析。扫描过程仍可能短暂读到会话文件中的正文或工具输出字节，但缓存只按日期和模型保存Token计数、预计算费用、计价覆盖量，以及增量扫描所需的文件大小、修改时间、解析位置和累计高水位；会话文件路径只保存不可逆摘要，不保存原始行或内容。
- 可选的额度预测缓存只保存采样时间、剩余百分比和重置时间，不保存账号标识、任务标题或会话内容。
- 不读取浏览器 Cookie、API 密钥、登录令牌或完整磁盘内容。
- 不上传监控数据。
- 软件每天最多自动只读访问一次本项目的GitHub公开版本接口；手动检查或一键更新时会连接GitHub。请求不包含电脑监控、Codex额度、Token用量或历史记录。
- 可选的OpenAI服务状态模块访问 `status.openai.com` 公开状态接口；正常时每10分钟一次，异常或失败时每2分钟一次。原生网络读取失败时会单次调用macOS自带的安全下载工具重试，仍验证HTTPS证书且不形成常驻进程。请求不带账号、Cookie、Token或监控数据；关闭主页和Codex页的该模块后停止请求。
- 可选历史记录只保存性能汇总，以及随机生成的本次运行编号和启动、唤醒、退出标记；不保存对话内容。
- 额度、订阅、Token用量和任务活动不写入性能历史文件；本机Token增量缓存与额度预测样本使用上文所述的独立最小字段文件。

Codex账户与任务列表正常或空闲时每5分钟、额度低于15%且有任务活动时每1分钟短暂启动本机Codex接口进程，读取完成后退出。连接失败按1、2、4、5分钟退避；不兼容或认证问题每5分钟重试。除上述GitHub更新和OpenAI官方服务状态请求外，本工具不直接向其他服务器发起请求；Codex本机接口可能按OpenAI客户端自身机制联网。
