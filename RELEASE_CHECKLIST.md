# 发布检查表

## 源代码首次公开前

- [x] 原生应用可从源码构建。
- [x] 日历用量逻辑测试通过。
- [x] 今天数据缺失、最近日期锚定和连续7天统计逻辑测试通过。
- [x] 电脑性能诊断通过。
- [x] Codex额度、账号和用量诊断通过；当前接口未返回5小时额度时会明确显示不可用。
- [x] 隐私、安全、贡献指南和MIT许可证已补齐。
- [x] 已检查项目文件，不包含个人路径、密钥、Cookie或登录令牌。
- [x] 真正最小化到Dock并恢复窗口的本机回归测试通过。
- [x] 内容完全隐藏、集中设置勾选状态、历史指标默认关闭和拖拽缩放逻辑通过本机自动化回归；集中设置窗口仍需补一张人工视觉验收截图。
- [x] 位置和大小锁定、隐藏模块停采、低唤醒定时器通过自动化回归。
- [x] 更新器版本比较、GitHub Release信息解析和SHA-256摘要校验通过自动化回归。
- [ ] 补充真实悬浮窗截图。
- [x] 在GitHub干净环境执行首次持续集成构建。
- [x] 已加入启动、唤醒、正常退出与实例标记；72小时可核验观察将在本次安装后开始累计。

## 0.8.0公开预览版

- [x] 本机构建同时包含Apple芯片和Intel处理器架构。
- [x] 当前Mac的电脑性能、Codex额度、账号和Token用量实机诊断通过。
- [x] 明确标注发布包尚未经过Apple开发者签名和公证。
- [x] 已创建首个GitHub Release；自动工作流、应用压缩包、SHA-256、签名、版本、双架构和应用内更新信息均已核验。

## 0.8.1缩放修复版

- [x] 横向、纵向、最大尺寸和真实窗口返回尺寸的等比约束测试通过。
- [x] 内部界面在调整大小后保持固定逻辑宽高，不再追随错误外框形状。
- [x] 当前Mac的真实边角拖拽和恢复标准大小视觉验收通过；内容比例、布局与缩放后按钮点击均已复核。
- [x] GitHub Release安装包、SHA-256、版本、双架构和应用内更新信息复核通过。

## 0.8.2更新交接修复版

- [x] 0.8.0到0.8.1的真实应用内更新已完成下载、校验、替换和新版本重启。
- [x] 该测试发现重启后LaunchAgent仍为“已退出”；0.8.2已改为优先通过LaunchAgent重启，并加入回归测试。
- [x] 当前Mac已实测从LaunchAgent“已退出”状态通过更新交接逻辑重新拉起，服务恢复为运行状态。
- [x] GitHub Release安装包、SHA-256、版本、双架构和启动服务交接信息复核通过。

## 0.9.0公开预览源码

- [x] 主页完整模块布局、长Token文字、Codex页隐藏模块后的自适应高度通过自动化回归。
- [x] 任意边角连续等比缩放、屏幕自适应最大尺寸、最小75%和位置大小锁定通过自动化回归。
- [x] 单实例文件锁、源码安装前退出旧实例和LaunchAgent异常退出自动拉起通过回归。
- [x] 当前Mac的电脑性能、Codex额度、账号、Token用量、任务历史和本机任务活动实机诊断通过。
- [x] 状态数据库最新工作目录优先和会话迁移/压缩降级逻辑通过固定测试；当前Mac 64个最近任务均返回工作目录，活动记录完整可读。
- [x] 72小时性能历史覆盖71.98小时，完整度96.4%，4个长缺口均有启动记录解释。
- [x] 设置页集中勾选、主页模块排序、数据来源和权限说明完成视觉检查。
- [x] 敏感信息、个人绝对路径、脚本语法、Plist、Markdown链接和版本号完成发布前检查。
- [x] 1.0不再以新增72小时观察为门槛；按用户决定改为本机诊断、既有历史和真实升级验证。
- [ ] 从已发布旧版本执行一次真实0.9.0应用内更新并复核LaunchAgent状态。

## 1.0正式发行前

- [x] 本机Token解析、标准价格、长上下文价格、未知模型、重复记录去重和增量缓存固定测试通过。
- [x] 费用卡片、额度预测卡片、独立显示开关、主页排序、长文字和完整主页布局回归通过。
- [x] 当前Mac真实会话扫描可用；低优先级流式解析后每64MB补齐轮次约2至3秒，计价覆盖率超过98%。
- [x] 官方7天、30天和月度Token汇总实机可用；本机模型样本采用累计高水位，重复快照与交错累计值不会反复放大。
- [x] 本机候选版后台运行正常：6秒空闲抽样为0%、2.9%、0%，常驻内存约59MB；64MB费用样本轮次约5秒，两轮单位费用率差异低于0.1%。
- [x] 用户决定取消新增72小时门槛；不再阻塞1.0。
- [x] 另一台受支持Mac的安装验证此前已经完成，用户确认1.0只需继续本机验证。
- [ ] 完成开发者签名与Apple公证，或明确只提供源码构建方式。
- [ ] 用0.8.2或后续版本升级到新版，复核安装包替换、安全校验和新版重启成功。
- [x] 登录后自动启动不作为1.0发布条件；新安装默认关闭，升级保留旧用户的已有选择。
- [x] 当前Codex版本下重新完成额度、账号、7天/30天用量、任务活动和最近任务实机诊断。
- [x] OpenAI官方服务状态解析、页面开关、主页排序、隐藏停请求和真实网络读取验证通过。

以上项目全部完成后才冻结1.0版本号、变更记录和发布包；冻结前不发布正式1.0。
