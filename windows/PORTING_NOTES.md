# Windows 复用边界与功能对照

审计基线：已冻结的 macOS 1.0 候选提交 `864ddea`。Windows 开发仅修改 `windows/`，没有改动 macOS 的 `overlay/`、构建脚本或发布产物。

## 技术边界

macOS 版的界面、平台采集和部分产品逻辑使用 Objective-C、Foundation 与 AppKit，不能直接在 Windows 编译。Windows 版复用已经验证过的信息架构、协议方法、刷新策略、计算口径和固定样例，并用 Win32/C++17 重建平台实现。

| macOS 部分 | Windows 当前处理方式 |
| --- | --- |
| 窗口、卡片、设置和模块排序 | 用 Win32 原生控件重建三页与十三个模块；主页和所属页分别控制显隐，支持置顶、任务栏最小化、拖动、自由缩放、纵向滚动、单实例和每显示器 DPI。 |
| CPU、内存、进程树、提交量和页面文件 | 用 Windows 系统接口重写；5 秒快采、20 秒慢采，串行后台执行并按页面/模块需求停采。 |
| Codex 账户、额度、用量和任务列表 | 用安全子进程与管道实现官方 `codex app-server --stdio`；只保留最小只读字段，按需启动并可取消。 |
| Token 日汇总 | 复用“缺失不等于零”的口径，用独立 C++ 日历算法计算今日或最近日期、近 7 日和近 30 日。 |
| Token 成本与预测历史 | 已用官方每日 Token 汇总作为总量、本机会话模型样本作为价格结构，显示近 30 天费用和月度预测；增量游标与冻结费用保存为 8 MiB 上限的版本化白名单缓存，损坏时回退到有界扫描。 |
| OpenAI 服务状态 | 已用 WinHTTP 和 Windows.Data.Json 实现；只在模块实际可见时按低频节奏读取官方公共状态。 |
| 自动更新 | 已实现每日/手动检查、安全下载、连续锁定的 MSI 校验安装事务和受控助手核心；仍需把临时助手复制与继承句柄启动接到真实按钮，并配置正式发布者证书指纹。 |
| macOS 签名、公证和安装 | 不可复用；Windows 需要安装包、Authenticode 代码签名、可信时间戳和独立更新流程。 |

## Codex 只读接口边界

Windows 当前每次刷新启动一个短生命周期 `codex.exe app-server --stdio`，先发送 `initialize` 和 `initialized`，再调用：

- `account/rateLimits/read`：五小时与每周额度；
- `account/read`：订阅类型；
- `account/usage/read`：官方每日 Token 汇总；
- `thread/list`：最近任务历史。

界面会保留上次成功值，并把单方法失败与其他方法隔离。成功后 5 分钟刷新；连续失败按 1、2、5、10、15 分钟退避并封顶，下一次成功后恢复 5 分钟正常刷新。最小化或没有可见 Codex 模块时取消在途读取并暂停。

任务状态有一个不可消除的语义限制：新启动的 `app-server` 只能提供它自身进程范围内的状态，不能证明 Codex 桌面版所有任务的全局实时状态。因此 Windows 版只把 `thread/list` 作为最近任务历史，界面明确标注状态范围，不显示或推算“桌面版当前共有几个任务在跑”。

为保护隐私，保留的数据模型不包含邮箱、任务标识、预览、工作目录、会话路径、提示词、回复或工具内容；原始 JSON-RPC 行和标准错误不会进入 UI 或落盘。

## 功能等价性

| 功能 | Windows 当前状态 | 关键限制或验证条件 |
| --- | --- | --- |
| 三页、十三个模块、主页/所属页独立开关和主页排序 | 已实现 | 设置格式已升级到版本 6，并有旧版迁移测试；仍需真实 Windows 下逐项操作验收。 |
| 五小时额度与每周额度独立选择 | 已实现 | 接口未返回某个窗口时保持“当前未返回”，不互相替代。 |
| 五小时与每周额度趋势 | 已实现第一版 | 复用现有额度刷新，按当前恢复周期近 24 小时历史判断；至少 3 点且跨度 15 分钟，仍需真实账号对照。 |
| 置顶、任务栏最小化、拖动、自由缩放、滚动和单实例 | 已实现 | 仍需真实多显示器、高 DPI、键盘和屏幕阅读器测试。 |
| 应用图标 | 已实现 | EXE 资源已加入多尺寸 `.ico`；仍需在任务栏、文件资源管理器和快捷方式中目视验收。 |
| 整机 CPU、物理内存、提交量、页面文件和内存排行 | 已实现第一版 | 固定算法测试已覆盖，仍需与真实任务管理器对照；多处理器组机器存在已知口径限制。 |
| Codex/ChatGPT 进程树 CPU 与内存 | 已实现 | 根进程按已知程序名识别；共享工作集只适合看趋势，真实安装样本仍需复核。 |
| 当前压力、瓶颈与 Codex/ChatGPT 合计影响诊断 | 已实现第一版 | 纯算法复用现有采样，不增加负担；数据不全时保持未知，仍需与真实任务管理器和体感对照。 |
| Codex 额度与订阅 | 已实现第一版 | 依赖本机可执行的、已登录的兼容 `codex.exe app-server`；真实账号对照待完成。 |
| 官方 Token 日汇总 | 已实现第一版 | 今日数据缺失时改显最近日期；近 7 日/30 日算法有边界测试，真实返回对照待完成。 |
| 最近任务历史 | 已实现第一版 | `thread/list` 请求上限为五条，界面最多显示三条名称和最近时间；进程范围状态不能当桌面版全局实时状态。 |
| `codex.exe` 自动发现 | 已实现 | 检查显式绝对路径、绝对 PATH 和固定用户级安装布局；最多 64 个候选，不递归、不用外壳、不访问 WindowsApps。真实安装布局仍需对照。 |
| 性能与 Codex 按需停采 | 已实现 | 页面/模块无需求或最小化时暂停并使旧结果失效；工作线程取消和无残留子进程已有集成测试，真实交互待验收。 |
| 网络速度、磁盘读写和历史曲线 | 未实现 | 需要 Windows 网络/磁盘计数器、睡眠/重启/网卡切换处理与本地历史格式。 |
| Token 月末趋势 | 已实现第一版 | 使用官方每日 Token 汇总做本月日均线性外推；数据不足不显示预测，不等于账单。 |
| Token 成本与费用预测 | 已实现 Beta | 默认关闭；官方总量优先，本地模型样本估价，未知模型降低覆盖率；重启增量缓存已实现，`.jsonl.zst` 仍未解压。 |
| OpenAI 服务状态 | 已实现第一版 | 固定请求官方公共状态页，不带账号信息；成功 15 分钟、失败退避，隐藏后停止后续请求并丢弃在途结果。 |
| 额度通知 | 未实现 | 需要默认关闭、用户阈值、自然日/滚动 24 小时和单周期防重复。 |
| 颜色、透明度、角落位置、锁定位置/大小 | 未实现 | 可移植产品规则，Win32 绘制和命中测试需要单独实现与视觉验收。 |
| 登录后自动启动 | 未实现 | 推荐当前用户范围且默认关闭，不要求管理员权限。 |
| 检查更新、一键更新和失败回滚 | 部分实现 | 每日与手动检查、严格下载、连续锁定的 MSI 安全事务和受控助手核心已完成。助手核对继承进程句柄的编号、创建时间和程序路径，等待旧 HUD 退出后安装；只在已安装 EXE 再次通过签名、固定发布者和版本身份校验后重启。仍需真实按钮下载两项资产、复制并验证临时助手、传递继承句柄并退出当前 HUD；正式发布者指纹未配置前必须保持关闭。 |
| 便携 ZIP 与 SHA-256 | 已实现 | Windows CI 打包 EXE、许可证和 Windows 说明，并独立回验哈希和包内文件；当前仍未签名。 |
| MSI 安装与卸载 | 已实现第一版 | 当前用户范围、不要求管理员权限；CI 会真实静默安装、核对 EXE 并卸载。真实桌面交互仍待验收。 |
| Windows 代码签名 | 未实现 | 正式发布需要 Authenticode 和可信时间戳；SmartScreen 信誉不能由一次签名测试证明。 |
| ARM64 | 未支持/未验证 | 当前 CI 与构建产物只覆盖 Windows x64 Release。 |
| macOS 内存压力 | 只能提供 Windows 等价判断 | Windows 可组合内存负载、可用量、提交限制和分页趋势，但不是同一指标。 |
| macOS 系统热压力 | 通用 Windows 版无法保证等价 | 没有适用于所有硬件的统一用户态接口；默认明确显示系统未提供，不伪造数值。 |

## 安全与资源边界

- `codex.exe` 必须是安全的绝对普通文件；自动查找限定固定目录、层数和候选数量。
- 不经命令行外壳启动；子进程只继承指定管道，并在运行前加入“关闭即清理”的 Windows Job。
- 单次接口读取有行大小、队列、总输出、标准错误和 15 秒总时限；响应洪水、卡死、取消和退出都必须回收进程树。
- 性能与 Codex 各自只有一个串行后台工作线程；窗口消息只接收已裁剪结果，不携带拥有所有权的裸指针。
- 设置仅保存白名单字段，并通过同目录临时文件替换，避免半写入状态。
- 更新助手不通过进程编号重新打开旧程序，而是消费启动方显式继承的进程句柄，并同时核对编号、创建时间和程序路径；等待失败不会进入 Windows Installer。
- MSI 安装失败不会启动任何文件；安装成功后仍要在锁住路径的情况下验证 EXE 的 Authenticode、固定发布者、产品名、原文件名和版本，再直接调用 `CreateProcessW`，助手自身不删除旧版程序文件。

## CI 与测试证据

Windows x64 CI 当前负责：

1. 运行源码契约检查；
2. 使用安装的 MSVC 配置并编译 Release；
3. 运行全部便携算法测试和 Windows 原生集成测试；
4. 验证并上传原始 `CodexMonitorHUD.exe`；
5. 生成、解包复核并上传便携 ZIP 与 SHA-256 校验文件；
6. 构建未签名 MSI，真实执行安装/卸载测试后上传 MSI 与 SHA-256。

新增测试覆盖十三个模块设置迁移、保守诊断口径、Codex 与服务状态 JSON 解析、安全进程启动与发现、官方握手和四个只读方法、乱序/异常/洪水响应、方法级旧值保留、Token 日期数学、额度趋势与白名单历史、本机会话有界扫描与费用混合口径、后台暂停取消、恢复刷新和无遗留子进程，以及更新助手的退出等待、失败短路、安装后 EXE 身份和未验证文件不启动。

这些证据可以证明源码契约、Windows x64 编译和固定输入行为，但不能替代真实用户机器上的交互、视觉、资源占用、账号数据和任务管理器对照。

## 下一阶段建议顺序

1. 先完成真实 Windows x64 交互验收：图标、三页、十三个模块开关、滚动、自由缩放、置顶、最小化、DPI、多显示器和设置恢复。
2. 用真实已登录 Codex 对照五小时/每周额度、订阅、Token 日期与汇总、最近任务历史，并核验主流安装渠道的 `codex.exe` 自动发现。
3. 对照任务管理器校准性能数据并记录空闲、可见、最小化三种状态的资源占用。
4. 再补网络、磁盘、性能历史和额度通知；Token 成本/预测与服务状态第一版已完成。
5. 完成颜色/透明度/锁定等产品设置后，再做更新器、安装包、Authenticode 签名和正式发布。
6. x64 稳定后单独建立 ARM64 构建、CI 和真实设备验证，不把“工具链可能编译”当作已支持。
